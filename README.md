# AdSkill

Plataforma SaaS para analisar anúncios, gerar copies, roteiros de TikTok Ads e VSLs com IA.

## Vercel

Adicione a variável de ambiente `OPENAI_API_KEY` no painel da Vercel para ativar a IA real.

## Scripts

- `npm run dev`
- `npm run build`
- `npm run start`
